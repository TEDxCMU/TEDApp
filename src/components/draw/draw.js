import React, {useState, useEffect} from 'react';
import '../../App.css';
import './draw.scss';

const Draw = (props) => {
    const [renderedCards, setRenderedCards] = useState([])

    useEffect(() => {
             
    }, [])

    return(
        <div>Hello</div>
    )
}

export default Draw;